<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Data</title>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
    }

    h1 {
        text-align: center;
        margin-top: 20px;
    }

    table {
        width: 100%;
       
        margin-top: 20px;
    }

    th,
    td {
        padding: 10px;
        text-align: left;
        border: 1px solid #ddd; /* Add border to each cell */
    }

    th {
        background-color: #f2f2f2;
        font-size: x-large;
        border-color: black;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    .truncate {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        max-width: 300px;
    }

    .expanded {
        white-space: normal;
        overflow: visible;
        text-overflow: initial;
        max-width: none;
    }

    /* Responsive adjustments for small screens (< 625px) */
    @media screen and (max-width: 624px) {

        th, td {
            padding: 8px;
            text-align: left; /* Remove borders for smaller screens */
        }

        th {
            font-size: x-large  ;
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:nth-child(odd) {
            background-color: #fff;
        }

        th::before {
            content: attr(data-label);
            font-weight: bold;
        }
    }
</style>
</head>

<body>
    <h1>All Posts</h1>
    <table cellspacing="10" cellpadding="10" border="1">
        <tr>
            <th>User Id</th>
            <th>UserName</th>
            <th>Preferred Code Language</th>
            <th>Standard Input</th>
            <th>Created At</th>
            <th>Source Code</th>
        </tr>
        {{#each posts}}
        <tr>
            <td>{{this.id}}</td>
            <td>{{this.username}}</td>
            <td>{{this.perferdCodeLang}}</td>
            <td>{{this.stdInp}}</td>
            <td>{{this.createdAt}}</td>
            <td>{{this.sourceCode}}</td>
        </tr>
        {{/each}}
    </table>
</body>

</html>